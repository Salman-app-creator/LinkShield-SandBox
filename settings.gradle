pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // FIX: JitPack add kiya for youtube-dl-android
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "LinkShield SandBox"
include ':app'
